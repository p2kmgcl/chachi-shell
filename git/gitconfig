[color]
  ui = auto

[user]
  name = Pablo Molina
  email = contact@pablomolina.me

[push]
  default = current

[core]
  editor = vim
  ignorecase = false
  pager = less -x4
  excludesfile = ~/.gitignore-global

[merge]
  tool = vimdiff

[help]
  autocorrect = 1

[alias]
  h = "! cat ~/.gitconfig"

  # List of shortcuts

  do-add-all = add .
  do-back-latest-commits = back
  do-fetch-all = fetch --all
  do-fetch-pull-request = pr
  do-remove-all-non-git-files = clean --force -d -X
  do-send-pull-request = pull-request
  do-short-log = log --pretty=format:'%cr%Cred%d%Creset%  %Cgreen[%h]%Creset %Cblue[%ce]%Creset %s' --decorate --graph --all
  do-ignore-changes-on-file = update-index --assume-unchanged

  br = branch
  bra = branch --all
  ch = checkout
  cm = commit
  ps = push
  rb = rebase
  rbi = rebase --interactive
  st = status
