version: '3.4'
services:
  freshrss:
    image: linuxserver/freshrss
    container_name: freshrss
    restart: unless-stopped
    volumes:
      - ./config:/config
    environment:
      PUID: 1000
      PGID: 1000
      TZ: Europe/Madrid
    ports:
      - 10002:80
