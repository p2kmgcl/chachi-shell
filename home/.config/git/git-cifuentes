#!/bin/bash
set -e
echo "Getting your master..."
git fetch --all --prune && git reset --hard origin/$(git branch --show-current) || exit 1
[ -x "$HOME/.cifuentes.post.local.sh" ] && "$HOME/.cifuentes.post.local.sh"
