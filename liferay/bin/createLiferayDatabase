#!/bin/bash

cd ~/Projects/chachi-shell/liferay/mysql && docker compose up --detach
echo "Waiting for healthy status..."
while [ "$(docker inspect -f '{{ .State.Health.Status }}' liferay_portal_database)" != "healthy" ]; do sleep 1; done
